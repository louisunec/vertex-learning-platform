# Clerk — https://dashboard.clerk.com → API keys
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up

# Sanity — https://www.sanity.io/manage → project → API
NEXT_PUBLIC_SANITY_PROJECT_ID=
NEXT_PUBLIC_SANITY_DATASET=production
NEXT_PUBLIC_SANITY_API_VERSION=2026-08-31
# Viewer token for server-only reads. Optional while the dataset is public;
# required once it is made private. Never exposed to the browser.
SANITY_API_READ_TOKEN=

# PostHog — project settings → project API key
NEXT_PUBLIC_POSTHOG_PROJECT_TOKEN=your_posthog_project_token_here
NEXT_PUBLIC_POSTHOG_HOST=your_posthog_host_here
# Server-only. Project Secret API key (phs_…) or Personal API key (phx_…) for
# local feature-flag evaluation. Optional: without it each flag check is a
# remote request; flags fail closed (off) on errors.
POSTHOG_SECRET_KEY=

# Search — server-side only, never exposed to the browser.
# SANITY_API_READ_TOKEN (above) is required for search: it authenticates the
# Sanity Context MCP (requires a deployed Studio).
# OpenAI API key for query interpretation; without it search degrades to
# deterministic keyword matching.
OPENAI_API_KEY=
# Slug of the published sanity.agentContext document scoping the Context MCP.
# Leave empty to use the unscoped base MCP endpoint.
SANITY_CONTEXT_SLUG=vertex-search
# Optional request-path timeout overrides in ms (1000–60000; invalid values use
# the defaults in lib/timeouts.ts: 10000 for the AI gateway, 8000 for the MCP,
# 20000 for the tutor's explanation call).
AI_GATEWAY_TIMEOUT_MS=
MCP_TIMEOUT_MS=
TUTOR_TIMEOUT_MS=

# Learner evidence (PR-4) — Supabase Postgres, server-side only, never exposed
# to the browser. Use the project's pooler connection string (Supabase →
# Connect → Transaction pooler). Also read by `npm run db:migrate`.
DATABASE_URL=
# Tests only: a disposable Postgres the database tests may create and drop
# databases on, e.g. postgres://postgres:postgres@localhost:54329/postgres.
# Without it those tests are skipped.
TEST_DATABASE_URL=

# Offline tooling only (npm run ingest:videos, npm run generate:assessments),
# never read by the app. Token needs Editor access to write `video` and draft
# `assessment` documents. Generation also uses OPENAI_API_KEY (above).
SANITY_API_WRITE_TOKEN=

# Learner progress — server-side only, read by POST /api/progress to save the
# signed-in learner's resume position and completion as `progress` documents.
# A separate token from the offline one above; needs write access (Editor).
# Without it progress saves return 503 and My Learning shows no new progress.
SANITY_API_PROGRESS_WRITE_TOKEN=

# Knowledge map — server-side only. Comma-separated Clerk user ids that also see
# AI-proposed prerequisite edges (unreviewed drafts, dashed) on the map. Display
# only: never used for gating, mastery, or next actions. Reads drafts with
# SANITY_API_READ_TOKEN. Leave empty to show approved edges only.
KNOWLEDGE_MAP_PROPOSED_EDGES_USER_IDS=
